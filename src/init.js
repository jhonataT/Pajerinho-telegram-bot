class InitGroup {
    constructor(groupData, userData){
        this.data = groupData;
        this.data = userData;
    }

    register(){
        // Verify if user is admin
        // Write group id in a json file
    }
}